apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: dev # Áp dụng cho namespace dev
spec:
  mtls:
    mode: STRICT # Bắt buộc tất cả giao tiếp bên trong phải có mTLS