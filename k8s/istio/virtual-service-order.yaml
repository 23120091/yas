apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: order-to-tax-retry
  namespace: production
spec:
  hosts:
    - tax
  http:
    - match:
        - sourceLabels:
            app.kubernetes.io/name: order
      route:
        - destination:
            host: tax
            port:
              number: 80
      retries:
        attempts: 3
        perTryTimeout: 2s
        retryOn: connect-failure,refused-stream,unavailable,503,5xx
      timeout: 10s
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: order-to-cart-retry
  namespace: production
spec:
  hosts:
    - cart
  http:
    - match:
        - sourceLabels:
            app.kubernetes.io/name: order
      route:
        - destination:
            host: cart
            port:
              number: 80
      retries:
        attempts: 2
        perTryTimeout: 2s
        retryOn: connect-failure,refused-stream,unavailable,503,5xx
      timeout: 10s
