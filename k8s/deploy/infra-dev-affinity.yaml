# Dev environment node affinity
# Pins dev infrastructure to dev worker node (spot instance, acceptable to crash)
env: dev
affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
        - matchExpressions:
            - key: env
              operator: In
              values:
                - dev
